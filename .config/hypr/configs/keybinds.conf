# KEYS
# bind - binde - bindle etc...
# l -> locked, aka. works also when an input inhibitor (e.g. a lockscreen) is active.
# r -> release, will trigger on release of a key.
# e -> repeat, will repeat when held.
# n -> non-consuming, key/mouse events will be passed to the active window in addition to triggering the dispatcher.
# m -> mouse, see below
# t -> transparent, cannot be shadowed by other binds.
# i -> ignore mods, will ignore modifiers.

# Mouse binds
bindm=SUPER,mouse:272,movewindow
bindm=SUPER,mouse:273,resizewindow

bind=,Caps_Lock,exec,sleep 0.2 && swayosd-client --caps-lock
bind=SUPER,P,exec,hyprctl --batch "dispatch pin active"
bind=SUPER,z,exec,hyprctl --batch "dispatch fakefullscreen active"
# bind=SUPER,P,exec,hyprctl dispatch dispatch && hyprctl dispatch pin
# bind=SUPER,P,exec,hyprctl --batch "dispatch togglefloating active; dispatch pin active"

# Terminals
bind=SUPER,Return,exec,kitty
bind=SUPER CTRL,Return,exec,kitty --class=catnip3
bind=SUPER ALT,Return,exec,kitty --config ~/.config/kitty/kitty_transparent.conf --class=kitlay
bind=SUPER,I,exec,firefox --new-window "$(wl-paste)"
# bind=ALT,Return,exec,st
# bind=SUPER,Return,exec,wezterm

# wl-gammarelay
# gamma blue/red UP and DOWN
binde=SUPER,minus,exec, busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n -100
binde=SUPER,Equal,exec,busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateTemperature n +100
# RESET
binde=SUPER,0,exec,busctl --user set-property rs.wl-gammarelay / rs.wl.gammarelay Brightness d 1.0 && busctl --user set-property rs.wl-gammarelay / rs.wl.gammarelay Temperature q 6500
# Brightness ctrl
binde=SUPER SHIFT,minus,exec,busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateBrightness d -0.02
binde=SUPER SHIFT,Equal,exec,busctl --user -- call rs.wl-gammarelay / rs.wl.gammarelay UpdateBrightness d +0.02

# Launchers
bind=CTRL SHIFT, P, exec, grim -g "$(slurp -d)" - | wl-copy
# bind=SUPER SHIFT, P, exec, grim -g "$(slurp -o -r -c '#ff0000ff')" - | satty --filename - --fullscreen --output-filename ~/Pictures/Screenshots/satty-$(date '+%Y%m%d-%H:%M:%S').png
bind=SUPER SHIFT, P, exec, grim -g "$(slurp -d)" /home/sweet/Pictures/screenshots/shot-$(date +"%F").png && notify-send "screenshot taken"
# bind=SUPER,O,exec,wofi --show drun -I
# bind=SUPER,O,exec,bemenu-run --fn 'Maple Mono NF 16' -i -c -W "0.${3:-6}" -B 3 -l 25 -p "Run: "
# bind=SUPER,O,exec,wofi --show drun -I --conf /home/sweet/.config/wofi/config -s /home/sweet/.config/wofi/macchiato_pink.css
bind=SUPER,O,exec,wofi --show drun -I --conf /home/sweet/.config/wofi/config -s /home/sweet/.config/wofi/pink.css

# bind=SUPER,space,exec,rofi -show drun -theme ~/github/hyprland-rofi/config.rasi
# bind=SUPERSHIFT,O,exec,rofi -show drun -theme ~/github/hyprland-rofi/config.rasi

# Keys
bind=SUPER,period,exec,killall wofi || printf $(cut -d ';' -f1 "$HOME/.config/rofi/emoji/emojis.txt" | wofi --dmenu | sed "s/ .*//") | wl-copy
bind=SUPER,X,exec,hyprpicker -a -n
# bind=SUPERSHIFT,K,exec,"$HOME/.config/hypr/scripts/keys.sh"

# Apps
bind=SUPER,E,exec,thunar
bind=SUPER,B,exec,$BROWSER
# bind=SUPER,C,exec,~/scripts/record-wofi.sh
bind=SUPER,C,exec,wf-recorder -g $(slurp -d)

# OBS
# bind = SUPER,F10,pass,^(com\.obsproject\.Studio)$

# TTS and Scrcpy
# bind=SUPER,T,exec,gtts-cli -- "$(wl-paste)" | play -t mp3 -
# bind=SUPER,T,exec,edge-tts -t "$(wl-paste)" | play -t mp3 -
# bind=SUPER,T,exec,edge-playback -t "$(wl-paste)"

# tts paste
# bind=SUPER,T,exec,"$HOME/scripts/amy" -t "$(wl-paste| sed 's/[^[a-zA-Z1-9]-]//g'|tr -d \'\" | perl -pe 's/[^[:ascii:]]//g')"
bind=SUPER SHIFT,T,exec, echo "$(wl-paste| sed 's/[^[a-zA-Z1-9]-]//g'|tr -d \'\" | perl -pe 's/[^[:ascii:]]//g')" | piper-tts --model /home/sweet/ssd/pipertts/ivona-8-23/amy.onnx --output_raw | aplay -r 22050 -c 1 -f S16_LE -t raw

bind=SUPER,T,exec,pgrep balcon.exe && pkill balcon.exe || echo "$(wl-paste| sed 's/[^[a-zA-Z1-9]-]//g'|tr -d \'\" | perl -pe 's/[^[:ascii:]]//g')" | "$HOME/scripts/balcon" -n Amy -i -s 5
bind=SUPER SHIFT,T,exec,pkill -SIGSTOP balcon.exe||pkill -SIGCONT balcon.exe  
bind=SUPER,R,exec,[floating:1;pin;size: 40% 40%;move 59% 5%] notify-send "pasting link..." && mpv --ytdl-format=bestvideo+bestaudio/best --speed=1 "$(wl-paste)"

# bind=SUPERSHIFT,T,exec,[floating:1;pin;size: 20% 20%;move 59% 5%]kitty "$HOME/scripts/ttsx" -t "$(wl-paste| sed 's/[^[a-zA-Z1-9]-]//g'|tr -d \'\" | perl -pe 's/[^[:ascii:]]//g')"
# bind=SUPER SHIFT,T,exec,[floating:1;pin;size: 20% 20%;move 59% 5%]"$HOME/scripts/etts.sh"
# bind=SUPER ALT,S,exec,[floating:1;pin;size: 50% 50%;move 25% 25%] kitty --hold -e /home/sweet/scripts/nf
# bind=SUPER,z,exec,[floating:1;pin;size: 40% 40%;move 59% 5%] kitty notify-send "lol"

# bind=SUPER,N,exec,env -u WAYLAND_DISPLAY neovide
# bind=CTRLSUPER,Y,exec,recommended.sh
# bind=SUPER,Y,exec,youtube.sh
# bind=SUPER,A,exec,tsuki
# bind=SUPER,T,exec,rofi-gpt
# bind=SUPER,M,exec,lobster
# bind=SUPER,G,exec,rofi -show filebrowser
bind=SUPER,U,exec,rofi -show drun

# Media keys
bindl=,XF86AudioRaiseVolume,exec,swayosd-client --output-volume raise && pamixer -i 5 && volume.sh
bindl=,XF86AudioLowerVolume,exec,swayosd-client --output-volume lower && pamixer -d 5 && volume.sh
bindl=,XF86AudioMute,exec,swayosd-client --output-volume mute-toggle && pamixer -t && volume.sh

bindl=,XF86AudioPlay,exec,playerctl -i firefox play-pause
bindl=,XF86AudioNext,exec,playerctl -i firefox next
bindl=,XF86AudioPrev,exec,playerctl -i firefox previous

# same thing but without fn keys
bind=,95,exec,playerctl -i firefox play-pause
bind=,96,exec,playerctl -i firefox next
bind=,75,exec,playerctl -i firefox previous

# Clipboard history
bind=SUPERSHIFT,period,exec,cliphist list | wofi -d | cliphist decode | wl-copy

# Screenshot
# $screenshotarea = hyprctl keyword animation "fadeOut,0,0,default"; grimblast --notify copy area; hyprctl keyword animation "fadeOut,1,4,default"
# bind=SUPERSHIFT,S,exec, $screenshotarea
# bind=,107,exec,screenshot.sh fullscreen
# bind=SHIFT,107,exec,screenshot.sh selectarea
# bind=CTRLSHIFT,107,exec,screenshot.sh quickedit
# bind=SUPER,107,exec,screenshot.sh quickcote
# bind=SUPERSHIFT,107,exec,screenshot.sh save

# disabling binds for csgo competitive gaming 🤓😎
# bind=SUPERSHIFT,127,submap,clean
# submap=clean
# bind=CTRLSHIFT,127,submap,reset
# submap=reset

# Misc
# bind=ALT,M,exec,notify-send "Pasting link into mpv..." && mpv $(wl-paste)
bind=SUPERSHIFT,L,exec,swaylock
bindl=SUPERALT,R,exec,reboot

# window management stuff
bind=CTRLSUPERALT,M,exit,
bind=SUPER,Q,killactive,
bind=SUPER,V,togglefloating,
bind=ALT,F,fullscreen,
bind=SUPER,F,fullscreen,
# bind=SUPER,P,pseudo,

# moving between workspaces
bind=SUPER,h,movefocus,l
bind=SUPER,l,movefocus,r
bind=SUPER,left,movefocus,l
bind=SUPER,right,movefocus,r
bind=SUPER,k,movefocus,u
bind=SUPER,j,movefocus,d

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,S,togglespecialworkspace,

# moving windows between workspaces
bind=ALT,1,movetoworkspace,1
bind=ALT,2,movetoworkspace,2
bind=ALT,3,movetoworkspace,3
bind=ALT,4,movetoworkspace,4
bind=ALT,5,movetoworkspace,5
bind=ALT,6,movetoworkspace,6
bind=ALT,7,movetoworkspace,7
bind=ALT,8,movetoworkspace,8
bind=ALT,9,movetoworkspace,9
bind=ALT,S,movetoworkspace,special

# resize
bind = SUPER CTRL, h, resizeactive, -100 0
bind = SUPER CTRL, l, resizeactive, 100 0
bind = SUPER CTRL, k, resizeactive, 0 -100
bind = SUPER CTRL, j, resizeactive, 0 100

# switch windows
bind = SUPER,N,swapnext
bind = SUPER,M,swapnext,prev
# bind = SUPER SHIFT,S,swapactiveworkspaces, 0 1
